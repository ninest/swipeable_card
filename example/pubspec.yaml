name: example
version: 1.0.0
homepage: https://github.com/themindstorm/swipeable_widget	
author: themindstorm

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  swipeable_widget:

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  swipeable_widget:
    path: ../

flutter:
  uses-material-design: true