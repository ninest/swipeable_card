import 'package:flutter/material.dart';

class SwipableWidget extends StatelessWidget {
  const SwipableWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is the swipable widget"),
    );
  }
}
